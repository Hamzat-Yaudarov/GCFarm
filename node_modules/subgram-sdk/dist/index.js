"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __knownSymbol = (name, symbol) => {
  return (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};
var __await = function(promise, isYieldStar) {
  this[0] = promise;
  this[1] = isYieldStar;
};
var __asyncGenerator = (__this, __arguments, generator) => {
  var resume = (k, v, yes, no) => {
    try {
      var x = generator[k](v), isAwait = (v = x.value) instanceof __await, done = x.done;
      Promise.resolve(isAwait ? v[0] : v).then((y) => isAwait ? resume(k === "return" ? k : "next", v[1] ? { done: y.done, value: y.value } : y, yes, no) : yes({ value: y, done })).catch((e) => resume("throw", e, yes, no));
    } catch (e) {
      no(e);
    }
  };
  var method = (k) => it[k] = (x) => new Promise((yes, no) => resume(k, x, yes, no));
  var it = {};
  return generator = generator.apply(__this, __arguments), it[__knownSymbol("asyncIterator")] = () => it, method("next"), method("throw"), method("return"), it;
};

// src/index.ts
var src_exports = {};
__export(src_exports, {
  default: () => src_default
});
module.exports = __toCommonJS(src_exports);

// src/subgram.ts
var import_axios = __toESM(require("axios"));
var Subgram = class {
  constructor(token) {
    this.BASE_URL = "https://api.subgram.io";
    this.apiToken = token;
  }
  get client() {
    return import_axios.default.create({
      baseURL: this.BASE_URL,
      timeout: 1e4
    });
  }
  hasAccess(userId, productId) {
    return __async(this, null, function* () {
      var _a;
      try {
        const response = yield this.client.get(`/api/v1/${this.apiToken}/products/${productId}/${userId}`);
        if (response.status !== 200)
          return false;
        const subscriptionStatus = response.data;
        return (_a = subscriptionStatus.has_access) != null ? _a : false;
      } catch (error) {
        console.error(`Error in hasAccess: ${error}`);
        return false;
      }
    });
  }
  createCheckoutPage(productId, userId, name, languageCode) {
    return __async(this, null, function* () {
      const response = yield this.client.post(`/api/v1/${this.apiToken}/products/${productId}`, {
        user_id: userId,
        name,
        language_code: languageCode
      });
      return response.data;
    });
  }
  listenToEvents(timeout = 2) {
    return __asyncGenerator(this, null, function* () {
      var _a;
      let eventId = 0;
      while (true) {
        try {
          const response = yield new __await(this.client.get(`/api/v1/${this.apiToken}/events`, { params: { event_id: eventId } }));
          const pollingResponse = response.data;
          for (const event_data of (_a = pollingResponse.result) != null ? _a : []) {
            const event = event_data;
            eventId = event.event_id;
            yield event;
          }
        } catch (error) {
          console.error(`Subgram API is not available, reason: ${error}`);
          yield new __await(new Promise((resolve) => setTimeout(resolve, timeout * 1e4)));
          continue;
        }
        yield new __await(new Promise((resolve) => setTimeout(resolve, timeout * 1e3)));
      }
    });
  }
};

// src/index.ts
var src_default = Subgram;
//# sourceMappingURL=index.js.map